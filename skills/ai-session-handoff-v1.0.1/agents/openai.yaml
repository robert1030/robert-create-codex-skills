interface:
  display_name: "AI Session Handoff"
  short_description: "Compact this session into a validated handoff another AI session can pick up."
  icon: "🧭"
  accent_color: "#2563EB"
  brand_color: "#2563EB"
  version: "1.0.1"
  argument_hint: "What will the next session work on?"
  default_prompt: "Use $ai-session-handoff to produce a validated handoff for continuing this work in a new session."

policy:
  allow_implicit_invocation: true
