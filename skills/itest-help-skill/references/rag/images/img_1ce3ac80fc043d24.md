---
{
  "image_chunk_id": "img_1ce3ac80fc043d24",
  "image_path": "topics/images/robot_quickCall_after_export.png",
  "category": "screenshot",
  "dimensions": [
    575,
    801
  ],
  "ocr_status": "ok",
  "has_ocr_text": true,
  "referenced_by": [
    "topics/robot_verify_exported_library.htm"
  ],
  "usage_count": 1
}
---

# Image: topics/images/robot_quickCall_after_export.png

- category: screenshot
- dimensions: (575, 801)
- ocr_status: ok
- referenced_by: topics/robot_verify_exported_library.htm

此圖片分類為畫面截圖（screenshot），已使用 Tesseract OCR（英文語言包）擷取圖片內文字，內容如下（OCR 結果，非人工校對，可能含辨識誤差，尤其是低解析度或特殊字元）：

```
library TelnetQC.py - C:\ALL-DOCS\7.3.0-Source\iTest\Demo\Telnet robot expo... [i=l

File Edit Format Run Options Window Help

m aTestCommon import *

= library _Telnetgc (object)

Headline: The entry point to the test case
Description:

Set credentials: Username and password
seesion = iTestConnon() ._getSeesion()
resnonse = eval (‘session Sercredentiais()")
aTestComoa () -_veiteLog (response. steps_repore())

session = iTestCommon()._getSession()

response = eval('session.ShowEnv()')
HTest Connon (J wei teLog (Fesponse, stepS FEpOTE (Y)
recurn iTestCommon(). fornResponse (response)

Description:
Show VLAN

session = iTestComnon() ._getSession()

response = eval ('session.ShowVLAN()')

iTestComnon () ._w2iteLog (response. steps_repert ())

recurn iTestComnon(). formResponse (response)

Ger Show0sers (seiF

Description:
Show users

session = iTestCommon()._getSession()

response = eval ('session.ShowUsers()')

iTestComnon (J writeLog (response steps| report ())

=evurn aTestCommon ()._formResponse (response)

i

Ln: Colo
```
