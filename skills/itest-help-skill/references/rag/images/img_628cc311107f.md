# 圖片：EncryptCmdInTheScript.png

- 分類：screenshot
- 判斷依據：尺寸較大(471x303)，OCR 辨識到 30 個文字詞，判定為含 UI 文字的畫面截圖
- 尺寸：471x303
- 引用來源 chunk：c_e63169d40e0d（topics/slcPasswordEncryptCommand.htm）
- OCR 文字預覽：import sys
from SpirentSLC import SLC

def my_testcase(psurd)

with SLC.init(host="localhost:9005") as slc:
ssh = slc.sessions.ssh.open(properties = {
“ipAddress’: ‘velocity-itest3.xored.com’,

“user*: "root"
“password’ :SLC.encrypt (pswrd),
DD)

if _name__ == "_main_':

my_testcase(sys.argv[@])
