---
{
  "chunk_id": "action_then__structure_d7ed03b57dace87f",
  "source_file": "topics/action_then.htm",
  "source_original_path": "topics/action_then.htm",
  "toc_path": [
    "iTest Online Help",
    "Controlling execution flow: Loops, If/Then, and Switch",
    "If / then / else logic",
    "The ‘then’ action: Element of an if/then construct (Tcl)"
  ],
  "heading_path": [
    "The ‘then’ action: Element of an if/then construct (Tcl)",
    "The ‘then’ action: Element of an if/then construct (Tcl)",
    "Structure"
  ],
  "anchor": "1518705",
  "context_ids": [
    "action_then"
  ],
  "index_keywords": [
    "Element of an if/then or if-elif-else construct” on page 325",
    "if-then-else",
    "in if constructs",
    "then",
    "then action"
  ],
  "index_keyword_paths": [
    "actions > then",
    "if-then-else",
    "then action",
    "then action > in if constructs",
    "“The ‘if’ action > Element of an if/then or if-elif-else construct” on page 325"
  ],
  "related_links": [
    "action_if.htm#1518551"
  ],
  "images": [],
  "content_hash": "d7ed03b57dace87f",
  "level": 3
}
---

# The ‘then’ action: Element of an if/then construct (Tcl) > The ‘then’ action: Element of an if/then construct (Tcl) > Structure

A legal if construct is made up of a contiguous sequence of:

One if step

Followed by one then step (or nested steps) The ‘if’ action: Element of an if/then or if-elif-else construct

Followed by zero or more elseif steps (or nested steps)

Followed by zero or one else step (or nested steps)

Comments are allowed at any point. Any other sequence is illegal. No other type of step can be interleaved in the sequence.
