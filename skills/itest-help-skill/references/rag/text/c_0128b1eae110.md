# Actions > Actions for CLI session types > iTest EXEC actions, grouped by function > Actions that read from and write to files

The ‘readFile’ action: Return the contents of a file

The ‘writeFile’ action: Write information to a file
