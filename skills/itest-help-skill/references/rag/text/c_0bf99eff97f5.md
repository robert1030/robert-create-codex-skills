# Python Sessions > Setting preferences for Python > Spirent > Session Types > Python

Note This is the path used by all sessions, when no path is provided in the Session profile.

- **Auto-select**：Indicates that iTest will select the Python interpreter from the “Path” environment variable.
- **Use specified Python interpreter**：Indicates that iTest will select the Python interpreter from the path specified, for example, C:\Python36\python.exe

Note This is the path used by all sessions, when no path is specified in the Session profile.
