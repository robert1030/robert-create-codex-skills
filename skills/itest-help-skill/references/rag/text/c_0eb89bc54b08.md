# iTest Commands > File and directory management commands > file mkdir command: Add a directory > Error cases

An error occurs if the directory does not already exist and a new directory could not be created.
