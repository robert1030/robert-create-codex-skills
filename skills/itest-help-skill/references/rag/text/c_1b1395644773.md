# iTest Topology Editor > Velocity command > Commands that return information from Velocity > velocity command syntax > setReservation > Note The findReservation command returns a list of pair<id, name>, but only the id is required. For example, in order to list the id of the first reservation in list, use: eval "lrange [split $reservation] 0 0".In Python use: Example usage:

Tcl: velocity setReservation -reservationId $reservationId

Python: velocity('setReservation', '-reservationId', $reservationId)
