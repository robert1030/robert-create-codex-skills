# iTest Commands > Commands that are commonly used in field replacements > response command: Accessing response data that is stored in a variable > Storing response data in a variable

See Storing a response into a variable (for use later in the test).
