# iTest Topology Editor > Velocity command > Commands that return information from Velocity > velocity command syntax > getReservationDetails subcommand > Python syntax:

velocity('getReservationDetails', '-reservationId', '<reservation_id>')
