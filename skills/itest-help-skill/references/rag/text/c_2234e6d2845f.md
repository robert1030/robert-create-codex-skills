# iTest Topology Editor > Velocity command > Commands that return information from Velocity > velocity command syntax > makeReservation subcommand > Arguments:

| 欄位1 | 欄位2 |
| --- | --- |
| topologyName | Name of the topology to reserve (string, required) |
| duration | Reservation duration in minutes (number, required) |
| Name | Name of the reservation (string, optional) |
| priority | Optional argument. Priority level available to the use: LOWEST, LOW, MEDIUM, HIGH, HIGHEST |
