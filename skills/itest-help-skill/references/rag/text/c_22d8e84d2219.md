# HTTP Sessions > HTTP commands > get page

get page fetches the contents of the specified URL and places it into the body of the response.

The get page command supports cookies as described in RFC 2965.
