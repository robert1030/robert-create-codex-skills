# JSON Editor > JSON Command in Test Case > JSON Command Syntax in Test Cases

The following shows JSON command example for Tcl and Python. For both languages, if you do not need to use substitution, escape [ ] with \ [ ] and use variable syntax as follows.
