# iTest Commands > File and directory management commands > file isFile command: Determine whether a URI represents a filename

Returns 1 (one) if the URI exists and is a filename, otherwise returns 0 (zero)
