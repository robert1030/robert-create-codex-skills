# OpenStack Neutron Session > OpenStack command set > While working on an interactive mode in the Session Editor

For OpenStack Neutron session, a Session Command button appears (top of the window), click the button and select a command.

![](images/OpenStack_capture-mode.PNG) <!-- image_ref -->
