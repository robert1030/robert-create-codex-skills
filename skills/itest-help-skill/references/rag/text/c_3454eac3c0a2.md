# Chat Sessions (XMPP chat) > Sending and receiving XMPP chat messages during test execution > Creating Chat test cases

The easiest way to create chat steps in a test case is to save a captured Chat (XMPP) session to the test case. Here is a a iTest Chat (XMPP) session:

![](images/chat.1.jpg) <!-- image_ref -->

Here is an example Chat test case:

As with any test case step, you can modify the step properties as needed. For example, you could add field replacements to include response data in sendMessage text.

![](images/chat.2.jpg) <!-- image_ref -->
