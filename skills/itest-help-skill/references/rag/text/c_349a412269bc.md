# iTest Topology Editor > Velocity command > Commands that return information from Velocity > velocity command syntax > makeReservation subcommand > Note If the argument is not specified, default priority is used. Conditions (listed them separate from arguments):

Note Ensure that conditions are added after the arguments.

![*](bullet_black_small.png) <!-- image_ref -->

![*](bullet_black_small.png) <!-- image_ref -->

![*](bullet_black_small.png) <!-- image_ref -->

Each conditions must be within double quotes (“---”) Each conditions is paired with resource name and condition value: <resource_name>.cond=<condition_value> The next symbols must be represented using the back slash:

| 欄位1 | 欄位2 |
| --- | --- |
| Conditions | List of objects representing custom conditions for abstract resources. (list, optional) |
| Important '[', ']', '"' '\' |  |
