# About the iTest Window > About workspaces > Working outside of a workspace: itar files

iTest supports itar files to enable any iTest resource to work with any iTest file, even if it is not in a workspace. See Exporting test cases and other iTest documents.
