# iTest Topology Editor > Velocity command > Commands that return information from Velocity > velocity command syntax > cancelReservation subcommand > Example usage Python:

velocity("cancelReservation", "-reservationId", "reservationId")
