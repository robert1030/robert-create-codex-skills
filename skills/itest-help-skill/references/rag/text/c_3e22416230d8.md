# iTest Topology Editor > Overview: iTest Topologies > Add, edit, or remove a session configuration for a iTest topology device > 第3段

(Alternatively, select the device on the canvas. On the Properties view, click the Session tab and then click )
