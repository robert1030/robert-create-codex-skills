# iTest Topology Editor > Global Topology > Global topology > Specifying a topology as the Global topology

In the Project Explorer or Favorites view, right-click the topology and select Set As Global Topology.

To undo the setting, right-click the Global topology and select Clear Global Topology or specify that another topology is the Global topology.

The icon for the Global topology is marked to distinguish it from other topologies:

![](images/topologies_11.1.jpg) <!-- image_ref -->
