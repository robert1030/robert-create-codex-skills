# iTest Topology Editor > Velocity command > Commands that return information from Velocity > velocity command syntax > findReservations subcommand > Example usage:

Tcl: velocity findReservations -status ACTIVE -name YK1 -endTime t60

Python: velocity("findReservations", "-status", "ACTIVE" "-name", "YK1", "-endTime", "t60")
