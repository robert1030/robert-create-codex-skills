# iTest Runtime: iTestRT > Overview: iTestRT (iTest Runtime) > Running iTestRT

To execute a iTest test case from the command line, you enter a command of the following form:

```
itestrt --test <testCaseFile>
```
