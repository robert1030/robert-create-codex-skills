# Actions > Actions for CLI session types > The ‘scriptSet’ action: Set the value of a variable (Tcl or a selected interpreter) > Response

The response to a scriptSet step is the string representation of the variable value.
