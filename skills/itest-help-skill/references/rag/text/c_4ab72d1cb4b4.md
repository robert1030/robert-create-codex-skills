# iTest Commands > File and directory management commands > file delete command: Delete files > Optional arguments

-r — Recursive (optional). Deletes a subdirectory recursively for the specified URI.
