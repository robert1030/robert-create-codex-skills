# Making your test case thread-safe > signalWaitAll: Wait until all specified events have been signaled or activated > Timeout

If a step times out, iTest generates an OnStepTimeout event and execution continues past the signalWaitAll step.
