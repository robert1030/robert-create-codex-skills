# Sharing iTest Resources > Exporting iTest projects as iTar files, Network DevOps Agent, or Velocity > Publish into Network DevOps Agent > 第1段

Select project(s), option Publish into Network DevOps Agent and Encrypt exported iTars, if required. See Encrypt exported iTars.

![](images/export_itar_select_publisIntoNDO.png) <!-- image_ref -->

Click Next and the page opens the Publish into Network DevOps Agent window as shown below.

![](images/export_itar_publishIntoNDO.png) <!-- image_ref -->

Note The Server URL, username, and password will be saved and populated for subsequent exports into Network DevOps Agent server.

Note As the Server URL, username, and password are saved, iTest verifies the URL connection for subsequent exports into Network DevOps Agent server.

![*](bullet_black_small.png) <!-- image_ref -->

![*](bullet_black_small.png) <!-- image_ref -->

![*](bullet_black_small.png) <!-- image_ref -->

iTars are skipped: The iTars exists on the Network DevOps Agent and is up to date (modification date/ETag is current). No disk Space: The iTars could not be downloaded due to inadequate disk space on the Network DevOps Agent. iTars rejected: the agent has been configured to run only digitally signed archives, and this archive does not contain a trusted, non-expired digital signature

![*](bullet_black_small.png) <!-- image_ref -->

![*](bullet_black_small.png) <!-- image_ref -->

No validation: (default) When selected iTest performs security validation using the default trust store. Validate certificate through PKIX: When selected, iTest performs security validation using your custom key store Note
