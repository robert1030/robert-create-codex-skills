# iTest Topology Editor > Velocity command > Commands that return information from Velocity > velocity command syntax

Tcl: velocity subcommand arg ?arg?

Python: velocity(velocity('subcommand', 'arg') #*args
