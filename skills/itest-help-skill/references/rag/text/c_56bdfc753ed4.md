# iTest Topology Editor > Velocity command > Commands that return information from Velocity > velocity command syntax > setReservation > Arguments:

| 欄位1 | 欄位2 |
| --- | --- |
| reservationId | Identification number of the reservation. For example, it can be the result of findReservations subcommand. (UUID, required) |
