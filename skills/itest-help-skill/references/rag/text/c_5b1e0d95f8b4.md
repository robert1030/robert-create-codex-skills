# Analysis Rules: Validating Responses > Analysis Rule Wizard: Extract page > XPath query applied to the response

This selection has the effect of setting What to extract (the Extractor property) to query for the resulting analysis rule.

Analysis rule wizard picks the XPath version from the TestCase and uses it for XPath evaluation on the Extract page.

1. 1 When you select this option, the wizard displays the structured data format of the response as it normally appears in the Structure view.

1. 2 Select the item of interest.
