# Actions > Actions for CLI session types > The ‘summarize’ action: Summarize the current test case execution results > Format of the response to a summarize step

Instructions for specifying the format of the response appear in Step 3 of To add a summarize step. You can specify the following response formats:
