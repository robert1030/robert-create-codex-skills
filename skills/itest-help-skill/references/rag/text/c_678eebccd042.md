# Sharing iTest Resources > Accessing iTest files that are held in iTar files > Working with encrypted iTars > Working with encrypted iTars in iTest GUI

iTest GUI filter out the encrypted projects from iTest GUI workspace and project artifacts will not be available in iTest GUI. That is, if a testcase refers to an encrypted content, it will fail execution in iTest GUI.
