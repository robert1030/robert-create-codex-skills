# iTest Commands > File and directory management commands > file isDirectory command: Determine whether a URI represents a folder name

Returns 1 (one) if the URI exists and is a directory, otherwise returns 0 (zero)
