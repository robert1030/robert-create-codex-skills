# iTest Topology Editor > Velocity command > Commands that return information from Velocity > velocity command syntax > url subcommand

Example Tcl: [velocity url] subcommand

Example Python: velocity('url') subcommand

The velocity url returns URL of the associated Velocity instance.

> **Note：** Note You may use [velocity] subcommands/velocity(subcommands) when authoring Velocity drivers.
