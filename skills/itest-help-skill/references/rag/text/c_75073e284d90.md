# iTest Topology Editor > Velocity command > Commands that return information from Velocity > velocity command syntax > findReservations subcommand

The findReservation command returns reservation list by filter.

> **Note：** Note The Velocity server limits the list of reservations returned to 200.
