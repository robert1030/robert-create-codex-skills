# iTest Commands > File and directory management commands > file exists command: Determine whether a file or folder exists

Returns 1 (one) if the file or directory exists, otherwise returns 0 (zero)
