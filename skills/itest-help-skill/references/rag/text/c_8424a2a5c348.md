# Popups（來源文件無 TOC 對應，依資料夾結構歸類） > popups/tbml_python.html > tbml

tbml('option', 'arg') #*args

The tbml group of commands returns information about resources from a topology (that is, information about a device, a link, or the topology itself).

See the online help for details on syntax, arguments, and return values.
