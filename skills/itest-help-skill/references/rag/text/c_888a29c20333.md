# Serial Sessions > Session profile property settings for Serial sessions > Terminal > Keyboard

- **Backspace**：Specify the code that the device interprets as the backspace character. Default: Ctrl-H
- **Enter**：Specify the code that the device interprets as the Enter character. Default: \r\n
