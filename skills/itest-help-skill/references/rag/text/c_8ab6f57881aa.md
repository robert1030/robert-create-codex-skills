# HTTP Sessions > HTTP session window > HTTP command

All HTTP commands return all of the HTTP headers from the response. Use Assert analysis rules to analyze HTML responses.

Use the ? character to view help for any HTTP command. For example, type get ? to view the options for the get command.

![](images/http.1.jpg) <!-- image_ref -->
