# Bash sessions > Bash session (Ubuntu and RHEL) > Note for Windows users:

Use the Command prompt sessions or the PowerShell sessions.

> **Note：** Note Bash (bash) is most commonly used in Unix shells.
