# Actions > Actions for CLI session types > The ‘scriptGet’ action: Get the value of a variable (Tcl or a selected interpreter) > Response

The response to a scriptGet step is the string representation of the variable value.
