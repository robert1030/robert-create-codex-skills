# Bash sessions > Session profile property settings for Bash sessions (Ubuntu and RHEL) > 1. Select the defaultRootPrompt checkbox. 2. In the More next command text box, specify the command characters (typically a space character) that cause the next page to appear. By default, a space character appears in the box. 3. In the More quit command text box, specify the command that exits the More display and returns to the command line prompt. By default, a q character appears in the box. 4. Specify a value for Terminal > Replay > Step Defaults > More. Terminal > Replay > Command

| 欄位1 | 欄位2 |
| --- | --- |
| Send interval between each character (milliseconds) |  |
