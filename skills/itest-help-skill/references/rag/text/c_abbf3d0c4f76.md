# Actions > Actions for CLI session types > General actions

The ‘message’ action: Add a severity type and message type

The ‘extractData’ action: Specify data tag and value to extract

The ‘teststep’ action: Export the test plan to a text file
