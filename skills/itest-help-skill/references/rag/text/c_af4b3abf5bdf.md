# Capturing Manual (Interactive) Sessions > Overview: Creating a test case by capturing interactive sessions > Inserting markers into the Capture view

All captured items appear in an ordered list in the Capture view. At any time, you can add a marker to the end of the current list.

When you save a captured session into a test case, makers are converted into comment actions.
