# Parameters > Merging parameter definitions from multiple sources > Previewing the runtime parameter settings while you develop a test case > Restrictions > Session Profile editors

The Parameters page displays merged parameters only from the current file (and its chain of inherited files). The Parameters page does not display the parameters that are merged from the Global parameters file
