# iTest Topology Editor > Velocity command > Commands that return information from Velocity > velocity command syntax > property subcommand: Return value of a property

The velocity property command returns the value of a property on a resource or on a link.

![*](bullet_blue.jpg) <!-- image_ref -->

- To identify a property, use its id or name
