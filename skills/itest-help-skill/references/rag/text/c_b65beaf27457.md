# iTest Topology Editor > Velocity command > Commands that return information from Velocity > velocity command syntax > makeReservation subcommand > Note You may user “store response” settings to store this reservation id. Example Tcl usage:

velocity makeReservation -topologyName "YK1" -duration 10 -name "reservation_MEDIUM" -priority "MEDIUM" "PC.cond=template\[PC\] and ports(integer\[Port Speed\]>=10000)>=2" "Server.cond=template\[Server\] and \[Hostname\]=\"xxxxx.xxxxx.com\""
