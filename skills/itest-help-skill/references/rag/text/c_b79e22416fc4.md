# Testing with Emulated Sessions > Adding a sample response (for use as an external emulation source) to an existing response map document

When you specify an external emulation source for an emulated response, iTest uses a sample response that is stored in a response map document. See Adding additional samples to a response map document for instructions on adding a sample response to a map for this purpose.
