# OpenStack Neutron Session > OpenStack command set > Session profile property settings for OpenStack session

The OpenStack session is built up by using REST QuickCall library, for more details on configuration of this session, see Session profile property settings for REST sessions.
