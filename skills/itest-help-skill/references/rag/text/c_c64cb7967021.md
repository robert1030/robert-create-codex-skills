# Parameters > Defining and managing parameters > Quick Facts: Where you can define and use parameters > Session profile (.ffsp) > Scope:

Visible only to the session steps that directly call the session profile or reference/inherit the session profile
