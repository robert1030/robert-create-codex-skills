# Analysis Rules: Validating Responses > Analysis rules: Properties of the extractor > Query extractor (for Global rules, Extract Using property = query) > Auto-generated queries

iTest auto-generates queries for JSON and TL1-format responses.

Mapping JSON responses

Mapping TL1 responses
