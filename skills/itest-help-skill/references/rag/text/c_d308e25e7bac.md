# iTest Runtime: iTestRT > Overview: iTestRT (iTest Runtime) > Example 3: Running a iTest job to schedule execution

To run a job from the command line, enter a command of the following form:

```
itestrt --job [jobUri]
```

For full details, see Running a job using iTestRT.
