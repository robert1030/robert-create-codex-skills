# Test Suites > Configuring a test suite: The General page of the Test Suite editor > Execution Behavior > 第2段

1. Note All test cases starting with "_" (the _setup and _cleanup test cases) are excluded from the test case filtering process. 3 You will now add as many test groups as are needed to define the test suite. Typically, you define a test group for each set of criteria that determine whether to execute or not execute a particular test case when the test suite runs. That is, you will define filters that determine which test cases from all specified folders should run.

![](images/test_suites.2.jpg) <!-- image_ref -->

To create a new test group and add it to the test suite, click Add . On the Test Group dialog box, supply a name for the test group and click OK. iTest adds the group to the Test Groups list.

1. 4 In the Test Groups list, double-click a group to configure its properties. We discuss adding and configuring a test group in Configuring a test group: The Test Group page.
