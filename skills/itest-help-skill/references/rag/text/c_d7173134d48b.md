# Making your test case thread-safe > killThread: Kill the specified threads > Creating a killThread step

![*](bullet_blue.jpg) <!-- image_ref -->

1. Add a step with an EXEC action of killThread.

1. 2 Specify the threads that the killThread step should kill: In the Command property, specify the threadName values of the threads. This can be a wildcarded list and can make use of field substitution.
