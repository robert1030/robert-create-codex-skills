# Run iTest as Network DevOps agent > Start and Stop NDO server > Network DevOps Agent API Calls > Executions using iTest NDO Agent

You may issue a POST /executions request, the iTest GUI launches in debug mode, loads the test case execution in the TestCase Editor.
