# Python Session Level Control Library > Encrypt password using SLC.encrypt(value) > Use SLC.encrypt() in your script

Example:

![](images/EncryptCmdInTheScript.png) <!-- image_ref -->

You may also use the iTest > Tools > Encrypt on the iTest GUI to generate Encrypted password and pasted into the SLC Script.
