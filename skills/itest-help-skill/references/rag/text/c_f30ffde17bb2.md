# Spirent TestCenter REST sessions > Spirent TestCenter REST session profiles > Commands available only for REST sessions > View commands on iTest Test Case Editor

Save the captured session actions as iTest Test Case.
