# iTest Runtime: iTestRT > iTestRT command reference > Usage

```
itestrt [options]
```

Options and arguments are fully described in the following tables.

> **Note：** Note Some options include a URI as an argument. When specifying a URI, use a single slash character after “file:”. For example:file:/C:/Workspace/my_project/<folder>/<filename>.<extension>
