# iTest Topology Editor > Velocity command > Commands that return information from Velocity > velocity command syntax > cancelReservation subcommand > Arguments:

| 欄位1 | 欄位2 |
| --- | --- |
| reservationId | Identification of reservation: (UUID, required) For example, UUID is the result of the makeReservation command |
