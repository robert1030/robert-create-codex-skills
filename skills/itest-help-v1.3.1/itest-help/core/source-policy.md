# 來源與引用政策

所有技術性答案必須附可追溯來源。內部知識庫來源使用下列格式：

```text
【知識庫來源】

- 檔案：
- 文件版本：
- 章節：
- 頁碼或位置：
- Chunk ID：
```

若沒有頁碼，填入 RAG 保存的來源位置，例如 DOM path 或原始檔案位置，並明示「未提供頁碼」。不可推測頁碼。

答案整合多個 Chunk 或文件時，逐筆列出。只引用實際用於支持該段結論的 Chunk，不得引用不存在或不相關的來源。
